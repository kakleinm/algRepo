#include <iostream>
using namespace std;

main()
{
    const int tam = 1;
    int v[tam], V[tam], soma[tam];
    cout << "primeiro valor: ";
    cin >> v[0];
    cout << "segundo valor: ";
    cin >> V[0];
    soma[0] = v[0] + v[0];
    cout << "resultado = " << soma[0];
}
